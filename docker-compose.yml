services:
  qdrant:
    image: qdrant/qdrant:v1.16.3
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - qdrant_storage:/qdrant/storage

  mcp:
    build:
      context: ./mcp
      dockerfile: Dockerfile
    ports:
      - "8000:8000"

volumes:
  qdrant_storage:
